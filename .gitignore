# ── SEGURIDAD (FUNDAMENTAL) ──────────────────────────────────────────
# Ignorar archivos de variables de entorno donde guardas tus claves
.env
.env.secret
.env.production

# ── FLUTTER/DART ─────────────────────────────────────────────────────
# Carpetas de compilación y dependencias
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/

# pubspec.lock SÍ se commitea en apps (garantiza versiones reproducibles)
# analysis_options.yaml SÍ se commitea (reglas de análisis compartidas)

# ── IDEs (ANDROID STUDIO / VS CODE) ──────────────────────────────────
.idea/
*.iml
*.ipr
*.iws
.vscode/
.history/
captures/

# ── ANDROID ──────────────────────────────────────────────────────────
# Solo archivos locales/compilados — la carpeta android/ SÍ se sube
.gradle/
local.properties
**/android/**/generated/
**/android/app/debug/
**/android/app/release/
*.keystore
*.jks

# ── OTROS ────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log

# Plataformas no usadas en este proyecto
windows/
linux/
macos/
web/

# Metadatos locales
.metadata
devtools_options.yaml
cosa.txt